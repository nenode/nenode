## Training Directory

**This directory contains the (old) training data (in `.json` format).**

---

Table of files/directories

|Names|Description|Location|
|-----|-----------|--------|
|New Training Data|the new data, new styling|[`../public/intents.json`](../public/intents.json)|
|Other Training Data|the extra/other data for _nenode_ training|[`../public/data.json`](../public/data.json)|
|Old Training|the old training data for _nenode_|[`OLD-training.json`](OLD-training.json)|

